name: Fetch latest worldwide data

on:
  repository_dispatch:
  schedule:
    - cron:  '00 * * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Check out this repo
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: 3.8
    - uses: actions/cache@v1
      name: Configure pip caching
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Install Python dependencies
      run: |
        pip install -r requirements.txt
    - name: Create temporary working directory
      run: |-
        mkdir -p tmp_worldwide && cd tmp_worldwide
    - name: Fetch latest worldwide data
      run: |-
        conditional-get --etags ../etags.json -v https://opendata.ecdc.europa.eu/covid19/casedistribution/json/
        jq . index.json > ../worldwide/worldwide.json
        conditional-get --etags ../etags.json -v https://raw.githubusercontent.com/CSSEGISandData/COVID-19/master/csse_covid_19_data/csse_covid_19_time_series/time_series_covid19_confirmed_global.csv
        mv time_series_covid19_confirmed_global.csv ../worldwide
        conditional-get --etags ../etags.json -v https://raw.githubusercontent.com/CSSEGISandData/COVID-19/master/csse_covid_19_data/csse_covid_19_time_series/time_series_covid19_deaths_global.csv
        mv time_series_covid19_deaths_global.csv ../worldwide
        conditional-get --etags ../etags.json -v https://raw.githubusercontent.com/CSSEGISandData/COVID-19/master/csse_covid_19_data/csse_covid_19_time_series/time_series_covid19_recovered_global.csv
        mv time_series_covid19_recovered_global.csv ../worldwide
    - name: Cleanup temporary working directory
      run: |-
        cd .. && rm -rf tmp_worldwide
    - name: Commit and push worldwide data if it changed
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest worldwide data: ${timestamp}" || exit 0
        git push
